# OpsCenter — переменные окружения. Скопируйте в .env и заполните.
# .env в .gitignore и никогда не коммитится.

# Ключ Google Gemini для LLM-разбора Inbox и OCR.
# Читается SDK google-genai напрямую из окружения (не хранится в config.py).
GEMINI_API_KEY=
